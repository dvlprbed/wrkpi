package lolu.wrkmbd;


public class raspberry {
    int test;
    /*
    import serial

gauche=5
droite=6

serialport = serial.Serial("/dev/ttyACM0", 9600,timeout=0.5)
btSerial = serial.Serial("/dev/rfcomm0", baudrate=9600, timeout=0.5)
while True:

        #if(serialport.readlines()=='5'):
        rcv = btSerial.read(512)
        valport =serialport.readline()
        if rcv:
                print(rcv)
                serialport.write(rcv)

        if valport:

                #print(valport)
                if (valport == "gauche"):
                                print valport
                                btSerial.write("20")
                elif (valport=="droite"):
                                print valport
                                btSerial.write("40")
                elif (valport=="avance"):
                                print valport
                                btSerial.write("30")
                elif (valport=="stop"):
                                print valport
                                btSerial.write("10")

                else :
                                print ("NON")
                #else :
                                #serialport.write("2")

        #response = serialport.readlines()=



     */
}
